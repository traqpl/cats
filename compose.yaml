services:
  cats:
    image: ${IMAGE_NAME:-cats:latest}
    container_name: cats
    restart: unless-stopped
    ports:
      - "${PORT:-8071}:8071"
    environment:
      PORT: "8071"
      DB_PATH: /data/cats_scores.db
    volumes:
      - cats-data:/data

volumes:
  cats-data:
